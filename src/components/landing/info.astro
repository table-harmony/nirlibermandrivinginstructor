---
import { Image } from 'astro:assets'

import { Separator } from '@/components/ui/separator'
---

<section id="info" class="bg-blue-100/65">
  <div class="container py-12 sm:py-20 lg:py-20">
    <div
      class="flex flex-col items-center justify-around space-y-10 md:flex-row"
    >
      <div class="flex flex-col items-center">
        <q class="mb-10 max-w-lg text-balance text-center text-sm md:text-base"
          >שמי הוא ניר ליברמן ואני מורה נהיגה שמלמד על גיר אוטומטי וידני. לימוד
          נהיגה ברמה אחרת, באופן מקצועי ללא פשרות ובשיטת לימוד ייחודית. שיעורים
          באוירה נינוחה וצעירה שמותאמת לכל אחד ואחת.</q
        >
        <Image
          src="/profile.jpeg"
          width="75"
          height="75"
          alt="profile"
          class="rounded-md"
        />
        <p class="pt-4 text-lg font-bold">ניר ליברמן</p>
        <span>מורה נהיגה בחיפה</span>
      </div>
      <Separator
        orientation="vertical"
        className="hidden h-64 bg-blue-400 md:block"
      />
      <Separator
        orientation="horizontal"
        className="block w-64 bg-blue-400 md:hidden"
      />
      <Image src="/electric-car.svg" width="500" height="500" alt="car" />
    </div>
  </div>
</section>

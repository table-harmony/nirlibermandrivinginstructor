---
import { siteConfig } from '@/config/site'

import { cn } from '@/lib/utils'
import { buttonVariants } from '@/components/ui/button'

import { BoldIcon } from 'lucide-react'
---

<section id="hero" class="container py-12 sm:py-20 lg:py-20">
  <div class="flex flex-col items-center space-y-6 text-center">
    <a
      href={siteConfig.links.bezeq}
      class={cn(buttonVariants({ variant: 'outline', size: 'sm' }))}
      target="_blank"
    >
      <span class="ml-3">🎉</span>
      היכרות ב{' '}
      <BoldIcon className="mr-2 size-4" />
    </a>
    <h1
      class="font-heading text-balance text-4xl sm:text-5xl md:text-6xl lg:text-7xl"
    >
      הדרך שלי -
      <span class="font-extrabold">ההצלחה שלך!</span>
    </h1>

    <p
      class="max-w-2xl text-balance leading-normal text-muted-foreground sm:text-xl sm:leading-8"
    >
      לימוד נהיגה ברמה אחרת, באופן מקצועי ללא פשרות ובשיטת לימוד ייחודית.
      שיעורים באוירה נינוחה וצעירה שמותאמת לכל אחד ואחת.
    </p>

    <div class="flex justify-center gap-4">
      <a
        href="#testimonials"
        class={cn(
          buttonVariants({
            variant: 'outline',
            size: 'lg',
          }),
        )}
      >
        ביקורות
      </a>
      <a href="#pricing" class={buttonVariants({ size: 'lg' })}> מחירים </a>
    </div>
  </div>
</section>

---
import MainLayout from '@/layouts/main-layout.astro'

import Gallery from '@/components/sections/gallery.astro'

import { siteConfig } from '@/config/site'

import { Button } from '@/components/ui/button'
import { MessageCircleIcon } from 'lucide-react'
---

<MainLayout title="ניר ליברמן - הודות" description="הודות - ניר ליברמן">
  <section class="container mb-28 mt-16 max-w-screen-md sm:mt-20 lg:mt-28">
    <div class="grid gap-8 sm:grid-cols-2">
      <div class="space-y-2">
        <h1 class="font-heading text-4xl font-bold">ניר ליברמן</h1>
        <p class="text-muted-foreground">מורה נהיגה</p>
      </div>
      <div>
        <p class="mb-10 text-base text-muted-foreground">
          היי! אני ניר ליברמן, מורה נהיגה שמלמד על גיר אוטומטי וידני.
        </p>
        <a target="_blank" rel="noreferrer" href={siteConfig.links.whatsapp}>
          <Button className="rounded-xl">
            <MessageCircleIcon className="ml-2 size-4" />
            תדברו איתי
          </Button>
        </a>
      </div>
    </div>
  </section>

  <Gallery />
</MainLayout>
